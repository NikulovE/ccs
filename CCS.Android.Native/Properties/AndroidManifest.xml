<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="1804160" package="com.ccs.android" android:installLocation="auto" android:versionName="GeoIP4U">
	<uses-sdk android:minSdkVersion="21" />
	<!-- We need to be able to download map tiles and access Google Play Services-->
	<!--uses-feature android:glEsVersion="0x00020000" android:required="true" -->
	<!-- We need to be able to download map tiles and access Google Play Services-->
	<uses-permission android:name="android.permission.INTERNET" />
	<!-- Allow the application to access Google web-based services. -->
	<!-- Google Maps for Android v2 will cache map tiles on external storage -->
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	<!-- Google Maps for Android v2 needs this permission so that it may check the connection state as it must download data -->
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<!-- These are optional, but recommended. They will allow Maps to use the My Location provider. -->
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<!-- Allow the application to access Google web-based services. -->
	<!--

	NOTE: you MUST follow the instructions on this page

	http://developer.xamarin.com/guides/android/platform_features/maps_and_location/maps/obtaining_a_google_maps_api_key/

	to get an API key for your specific debug.keystore SHA1 and package name.



	Update all the values below:

	* package name in the prefix of uses-permission element

	* package name in the prefix of permission element

	* API_KEY value obtained from https://console.developers.google.com/

	-->
	<!-- Notice here that we have the package name of our application as a prefix on the permissions. -->
	<permission android:name="com.ccs.android.permission.MAPS_RECEIVE" android:protectionLevel="signature" />
	<application android:label="Commute Car Sharing">
		<meta-data android:name="com.google.android.geo.API_KEY" android:value="AIzaSyDDBY4FgbRJgBVaitOb_cyqGWOwTIfVKIQ" />
		<meta-data android:name="com.google.android.gms.version" android:value="@integer/google_play_services_version" />
		<receiver android:name="com.google.firebase.iid.FirebaseInstanceIdInternalReceiver" android:exported="false" />
		<receiver android:name="com.google.firebase.iid.FirebaseInstanceIdReceiver" android:exported="true" android:permission="com.google.android.c2dm.permission.SEND">
			<intent-filter>
				<action android:name="com.google.android.c2dm.intent.RECEIVE" />
				<action android:name="com.google.android.c2dm.intent.REGISTRATION" />
				<category android:name="${applicationId}" />
			</intent-filter>
		</receiver>
	</application>
</manifest>