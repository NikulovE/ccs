<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             x:Class="CCS.MainPage"  Appearing="ContentPage_Appearing" Disappearing="ContentPage_Disappearing">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <!--<RowDefinition Height="Auto"/>-->
            </Grid.RowDefinitions>
            <StackLayout Orientation="Horizontal">
                <Button Text="Profile" Clicked="EditProfile" x:Name="S"/>
                <Button Text="Msgs" Clicked="CheckMessages"/>
                <Button Text="Find" Clicked="StartFindCompaions"/>
                <Button Text="Trips" Clicked="CheckTrips"/>
            </StackLayout>
            <maps:Map MapType="Street" x:Name="Map" Grid.Row="1" IsShowingUser="True">
            </maps:Map>

        </Grid>
    </ContentPage.Content>
</ContentPage>