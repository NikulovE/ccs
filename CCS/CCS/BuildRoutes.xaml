<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             x:Class="CCS.BuildRoutes" Title="Write routes">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                <Button Text="Put point to home" Clicked="PutRPToHome"/>
                <Button Text="Put point to work" Clicked="PutRPToWork"/>
            </StackLayout>
            <maps:Map MapType="Street" x:Name="Map" Grid.Row="1"  IsShowingUser="True">
            </maps:Map>

        </Grid>
    </ContentPage.Content>
</ContentPage>