<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             x:Class="CCS.SetHome" >
    <ContentPage.Content>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <!--<RowDefinition Height="Auto"/>-->
            </Grid.RowDefinitions>
            <Label Text="Set home" HorizontalOptions="Center" Margin="10" FontSize="Large"/>           
            <maps:Map MapType="Street" x:Name="GlobalMap" Grid.Row="1" IsShowingUser="True" />
            <Button Text="Move home to the center" HorizontalOptions="Center" Margin="0,5,0,0" Clicked="MoveToHomeToCenter" Grid.Row="1" VerticalOptions="Start" />
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" Margin="0,0,0,5" VerticalOptions="End"  Grid.Row="1">
                <Button Text="Save"  Clicked="SetHomeOnMap" />
                <Button x:Name="Continue" Text="Continue"  Clicked="BackToMainPage"  IsVisible="False"/>
            </StackLayout>
            

            <!--<Grid BackgroundColor="White" Grid.Row="1"></Grid>-->
            <!--<maps:Map 
            x:Name="MyMap"
            IsShowingUser="true"
            MapType="Hybrid"
        />-->
        </Grid>
    </ContentPage.Content>
</ContentPage>